.PHONY: all clean init build install run test lint docker-build docker-push


all: clean build test
	@echo "make target $@ invoked."

clean: 
	@echo "make target $@ invoked."

init: 
	@echo "make target $@ invoked."

build: 
	@echo "make target $@ invoked."

install: 
	@echo "make target $@ invoked."

run: 
	@echo "make target $@ invoked."

test: 
	@echo "make target $@ invoked."

lint: 
	@echo "make target $@ invoked."

docker-build: 
	@echo "make target $@ invoked."

docker-push: 
	@echo "make target $@ invoked."

# Empty last line